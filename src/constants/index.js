import * as actionsConstants from './actionsConstants';
import * as styleConstants from './styleConstants';
import * as valuesConstants from './valuesConstants';

export const {
  ADD_FAVORITE,
  ADD_CUSTOM_FAVORITE,
  SET_RATING,
  DELETE_FAVORITE,
  TOGGLE_NAVIGATION,
  CLOSE_NAVIGATION
} = actionsConstants;

export const {
  DANGER_COLOR,
  PRIMARY_COLOR,
  OVERLAY_BG_COLOR,
  ACTIVE_MARKER_SHADOW,
  MARKER_SHADOW,
  NAV_SHADOW,
  ACTIVE_NAV_SHADOW,
  DEFAULT_TRANSITION_DURATION,
  NAVIGATION_HEIGHT
} = styleConstants;

export const { BERLIN } = valuesConstants;
