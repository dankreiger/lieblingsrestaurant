import styled from 'styled-components';
import { Col } from 'reactstrap';

/* this code is here for jest testing and component readability */
export const NavigationTogglerCol = styled(Col)`
  display: flex;
`;

export const MapSearchInputCol = styled(Col)``;
export const AddressTypeSelectCol = styled(Col)``;
