import styled from 'styled-components';

export const AppContainer = styled.div`
  header {
    position: absolute;
    left: 15px;
    top: 1em;
    z-index: 1;
    a {
      padding-right: 10px;
    }
  }
`;
